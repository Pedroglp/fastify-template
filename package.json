{
  "name": "fastify-test",
  "version": "1.0.0",
  "description": "testing fastify",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "docker-compose up -d mongo && NODE_ENV=development node start.js",
    "stage": "docker-compose build && docker-compose up",
    "server:stage": "NODE_ENV=stage node start.js"
  },
  "keywords": [
    "js",
    "server",
    "test"
  ],
  "author": "Pedro Lancelloti",
  "license": "ISC",
  "dependencies": {
    "fastify": "^1.13.3",
    "fastify-mongodb": "^1.0.0",
    "mongoose": "^5.4.1"
  }
}
